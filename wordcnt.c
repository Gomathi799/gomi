#include<stdio.h>
void main()
{
    char a[100];
    int m=0,i;
    gets(a);
    for(i==0;a[i]!='\0';i++)
    {
        if(a[i]==' ')
        {
            m++;
        }
    }
    printf("%d",m+1);
}
